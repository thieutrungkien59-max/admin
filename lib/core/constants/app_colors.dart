import 'package:flutter/material.dart';

class AppColors {
  // Màu chính (Đỏ LogiRoute)
  static const Color primaryRed = Color(0xFFD32F2F); // Đỏ thẫm chuyên nghiệp
  static const Color primaryRedLight = Color(0xFFFFEBEE); // Đỏ nhạt cho menu được chọn
  
  // Màu nền tảng
  static const Color backgroundGray = Color(0xFFF4F6F8); // Xám nhạt (Nền Dashboard)
  static const Color white = Colors.white;
  
  // Màu Text
  static const Color textMain = Color(0xFF212121); // Đen xám cho tiêu đề
  static const Color textSubtitle = Color(0xFF757575); // Xám cho text phụ
  
  // Màu trạng thái
  static const Color statusGreen = Color(0xFF4CAF50); // Hoàn thành / Online
  static const Color statusOrange = Color(0xFFFF9800); // Đang giao / Cảnh báo
  static const Color statusRed = Color(0xFFF44336); // Lỗi / Offline
}